import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const BR = ({customHeight}) => {
  return (
    <View style={{height:customHeight}}>
    </View>
  )
}

export default BR